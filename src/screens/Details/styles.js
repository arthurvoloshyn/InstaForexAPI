import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  button: {
    marginTop: 50,
    width: 100,
  },
  container: {
    alignItems: 'center',
    flex: 1,
    paddingTop: 20,
  },
});

export default styles;
